class UserModel {
  String name;
  String uid;
  String profileId;
  num days;
  num coins;
  List<String> flowers;

  UserModel(this.name, this.uid, this.profileId, this.days, this.coins, this.flowers);
}